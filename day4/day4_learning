# DICTIONARY AND SETS

# Dictionary
# Dictionaries are used to store data values in key:value pairs.
# They are unordered, mutable(changeable) and don't allow duplicate keys .

'''info = {
    "name" : "punarv gupta",
    "subjects" : ["python", "C", "Java"],
    "topics" : ("dict", "set"),
    "age" : 12,
    "is adult" : True,
    "marks" : 94.4,
    12.99 : 94.4
}

print(info)'''

# Nested Dictionaries

'''student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student)

# The "myDict.keys()" is an function which returns all keys .
# For Example :


student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student.keys())'''

# The "myDict.values()" is an function which returns all values .
# For Example :

student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student.values())
# The "myDict.items()" is an function which returns all (key,val) pairs as tuples .
# For Examples :

student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student.items())

# The "myDict.get("key"") is an function which returns the key according to the value .
# For Example :

student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student.get("name"))

# The "myDict.update(newDict) is an function which inserts the specified items to the dictionaries.
# For Example :

student = {
    "name" : "rahul kumar",
    "subjects" : {
        "physics" : 97,
        "chemistry" : 98,
        "mathamatics" : 95
    }
}

print(student.update({"school" : "Jayshree Periwal High School"}))
print(student)


# SETS

# Set is the collection of the unordered items .
# Each element in the set must be unique and immutable .

collection = {1, 2, 3, 4}
print(collection)
print(type(collection))

# Sets ignore duplicate values :
# For Example :

collection={1, 2, 2, 2, "hello", "world", "world", 4}
print(collection)
print(type(collection))

# Let's find out how to create an empty set .

collection = set()
print(collection)
print(type(collection))

# Set methods

# The "set.add( el ) is an function which adds an element .
# For Example :

collection=set()
collection.add(1)
collection.add(2)
collection.add(2)

print(collection)

# The "set.remove( el ) is an function which removes the element .
# For Example :

collection=set()
collection.add(1)
collection.add(2)
collection.add(2)

collection.remove(1)
print(collection)

# The "set.clear()" is an function which empties the set .
# For Example :

collection=set()
collection.add(1)
collection.add(2)
collection.add("punarvgupta")
collection.clear()
collection.clear()
print(collection)
# The "set.pop" is an function which removes a random value.
# For Example :

